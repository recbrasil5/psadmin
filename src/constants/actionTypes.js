"use strict";

//keyMirror replaces the null with what is on the left side of the colon mark
var keyMirror = require('react/lib/keyMirror');

module.exports = keyMirror({
    INITIALIZE: null,
    CREATE_AUTHOR: null
});